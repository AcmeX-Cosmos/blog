# Quick Start

```bash
# 本地预览
npm run build && npm run preview

# 推送到 GitHub（自动触发 Pages 部署）
git add . && git commit -m "" && git push origin main
```

git add . && git commit -m "添加关于页面自瞄小游戏设计" && git push origin main