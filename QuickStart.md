# Quick Start

```bash
# 本地预览
npm run build && npm run preview

# 推送到 GitHub（自动触发 Pages 部署）
git add . ':!.gitignore' && git commit -m "" && git push origin main
```

git add . ':!.gitignore' && git commit -m "优化关于页关键词滚动动效，滑入滑出更流畅" && git push origin main


把刚才修改总结成20字的commit